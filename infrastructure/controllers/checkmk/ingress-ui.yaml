apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: checkmk-ui
  namespace: checkmk
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt"
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts: [ "checkmk.bacherik.de" ]
      secretName: checkmk-tls
  rules:
    - host: checkmk.bacherik.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: checkmk
                port:
                  name: http
