apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: reposilite
  namespace: reposilite
spec:
  interval: 5m
  chart:
    spec:
      chart: reposilite
      version: "1.3.25"
      sourceRef:
        kind: HelmRepository
        name: reposilite
        namespace: reposilite
