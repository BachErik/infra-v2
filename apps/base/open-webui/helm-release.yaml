apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ollama
  namespace: ollama
spec:
  interval: 5m
  chart:
    spec:
      chart: ollama
      version: "1.4.0"
      sourceRef:
        kind: HelmRepository
        name: ollama-helm
        namespace: ollama
  values:
    ollama:
      enabled: false